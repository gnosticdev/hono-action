{
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "release-it": "^19.0.4"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "name": "root",
  "packageManager": "bun@1.2.21",
  "private": true,
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write",
    "package:dev": "bun run --cwd package dev",
    "playground:dev": "bun run --cwd playground dev",
    "publish": "bun run release",
    "release": "bun --cwd package release-it",
    "release:major": "bun --cwd package release-it major",
    "release:minor": "bun --cwd package release-it minor",
    "release:patch": "bun --cwd package release-it patch",
    "typecheck": "bun --cwd package typecheck"
  },
  "version": "1.0.6",
  "workspaces": {
    "packages": [
      "package",
      "playground"
    ]
  }
}
